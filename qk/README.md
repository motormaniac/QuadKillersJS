# QuadKillers Version 2
Use vite typescript system for easier development

## Development:
### 1. Run the dev server
`npm run dev`
### 2. Typescript error watch (optional):
Run `npm run check` in a separate terminal which runs this under the hood: `tsc --watch --noEmit`
This will run in the background and constantly check for typescript errors.